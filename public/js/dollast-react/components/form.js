// Generated by LiveScript 1.4.0
(function(){
  var ref$, createClass, PropTypes, form, chapter, out$ = typeof exports != 'undefined' && exports || this;
  ref$ = require('react'), createClass = ref$.createClass, PropTypes = ref$.PropTypes;
  out$.form = form = createClass({
    displayName: 'form',
    propTypes: {
      submit: PropTypes.func.isRequired,
      id: PropTypes.string.isRequired,
      fields: PropTypes.object.isRequired
    },
    submit: function(e){
      var $form, allValues;
      e.preventDefault();
      $form = $("#" + this.props.id);
      allValues = $form.form('get values');
      return this.props.submit(allValues);
    },
    componentDidUpdate: function(){
      var $form;
      $form = $("#" + this.props.id);
      return $form.form({
        submit: this.submit,
        on: 'blur',
        fields: this.props.fields
      });
    },
    render: function(){
      return _('form', {
        className: "ui form segment",
        id: this.props.id
      }, _('div', {
        className: "ui error message"
      }), this.props.children);
    }
  });
  out$.chapter = chapter = createClass({
    displayName: 'fields',
    render: function(){
      return _('div', null, _('h2', {
        className: "ui dividing header"
      }, this.props.title), this.props.children);
    }
  });
}).call(this);

// Generated by LiveScript 1.3.1
(function(){
  var probApp;
  probApp = angular.module('dollast-prob-app', ["doffirst"]);
  probApp.controller('prob-show-ctrl', [
    "$scope", "doffirst", "$routeParams", function($scope, doffirst, $routeParams){
      $scope.pid = parseInt($routeParams.pid);
      return doffirst.get("/problem/" + $scope.pid, $scope);
    }
  ]);
  probApp.controller('prob-list-ctrl', [
    "$scope", "doffirst", "$routeParams", function($scope, doffirst, $routeParams){
      return doffirst.get("/problem", $scope);
    }
  ]);
  probApp.controller('prob-modify-ctrl', [
    "$scope", "doffirst", "$routeParams", function($scope, doffirst, $routeParams){
      var that, pid;
      if (that = $routeParams.pid) {
        pid = parseInt(that);
        doffirst.get("/problem/" + pid + "/total", $scope, function(it){
          return it._id = pid, it;
        });
      } else {
        $scope.prob = {};
        doffirst.get("/problem/next-count", $scope.prob);
      }
      return $scope.submit = function(){
        console.log($scope.prob);
        return doffirst.put("/problem/" + $scope.prob._id, $scope.prob);
      };
    }
  ]);
}).call(this);

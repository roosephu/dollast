// Generated by LiveScript 1.3.1
(function(){
  var createClass, connect, iconText, I, selector;
  createClass = require('react').createClass;
  connect = require('react-redux').connect;
  iconText = require('../elements').iconText;
  I = require('immutable');
  selector = function(state){
    return {
      user: I.fromJS({
        id: '',
        privList: []
      })
    };
  };
  module.exports = connect(selector)(createClass({
    displayName: 'user-modify',
    render: function(){
      var user, priv;
      user = this.props.user.toJS();
      return _('div', {
        className: "ui form"
      }, _('p', null, "user id: " + user.id), _('div', null, _('ul', null, (function(){
        var i$, ref$, len$, results$ = [];
        for (i$ = 0, len$ = (ref$ = user.privList).length; i$ < len$; ++i$) {
          priv = ref$[i$];
          results$.push(_('li', null, 'priv', _('input', {
            type: 'button',
            onClick: this.remove,
            value: remove
          })));
        }
        return results$;
      }.call(this)))), _(iconText, {
        className: "submit primary",
        icon: 'save',
        text: 'submit'
      }));
    }
  }));
}).call(this);

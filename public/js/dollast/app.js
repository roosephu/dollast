// Generated by LiveScript 1.4.0
(function(){
  var custom, co, redux, createClass, render, Router, Provider, routes, app, devtools, configureStore, actions, log, store;
  custom = require('./custom');
  co = require('co');
  redux = require('redux');
  createClass = require('react').createClass;
  render = require('react-dom').render;
  Router = require('react-router').Router;
  Provider = require('react-redux').Provider;
  routes = require('./components/routes');
  app = require('./components/app');
  devtools = require('./components/devtools');
  configureStore = require('./store').configureStore;
  actions = require('./actions');
  log = debug('dollast:app');
  store = configureStore();
  render(_('div', null, _(Provider, {
    store: store
  }, _('div', null, _(Router, null, routes(app)), _(devtools)))), document.getElementById('content'));
}).call(this);

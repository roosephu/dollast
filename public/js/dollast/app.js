// Generated by LiveScript 1.4.0
(function(){
  var custom, co, reflux, R, T, E, routes, navbar, footer, sess, app;
  custom = require('./custom');
  co = require('co');
  reflux = require('reflux');
  R = require('react/addons');
  T = require('react-router');
  E = require('./components/elements');
  routes = require('./components/routes');
  navbar = require('./components/site/navbar');
  footer = require('./components/site/footer');
  sess = require('./stores/sess');
  sess.actions.loadToken();
  app = R.createClass({
    mixins: [reflux.connect(sess.store, 'uid')],
    displayName: 'dollast',
    render: function(){
      console.log(this.state);
      return _div({
        className: "ui grid"
      }, _(navbar, this.state.uid), _div({
        className: "row"
      }, _div({
        className: "three wide column"
      }), _div({
        className: "ten wide column"
      }, _(T.RouteHandler))), _div({
        className: "row"
      }, _(footer)));
    }
  });
  T.run(routes(app), function(root){
    return R.render(_(root), document.getElementById('content'));
  });
}).call(this);

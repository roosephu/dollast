// Generated by LiveScript 1.3.1
var examsApp;
examsApp = angular.module('exams-app', []);
examsApp.controller('exams-app-ctrl', function($scope, $http){
  $scope.send = function(mode){
    var studentSol, i$, ref$, len$, prob;
    console.log(mode);
    studentSol = {};
    for (i$ = 0, len$ = (ref$ = $scope.problems).length; i$ < len$; ++i$) {
      prob = ref$[i$];
      studentSol[prob.id] = prob.sol;
    }
    console.log(studentSol);
    return $http.post("/api/exams/" + mode, studentSol).success(function(data, status, headers, config){
      return console.log(mode + " request received... but not implemented");
    });
  };
  return $http.get('/api/exams/get').success(function(data, status, headers, config){
    var i$, ref$, len$, prob, results$ = [];
    console.log(data);
    $scope.problems = data;
    for (i$ = 0, len$ = (ref$ = $scope.problems).length; i$ < len$; ++i$) {
      prob = ref$[i$];
      results$.push(prob.sol = 'A');
    }
    return results$;
  }).error(function(data, status, headers, config){
    console.log('Network Error');
    return $scope.problems = [];
  });
});